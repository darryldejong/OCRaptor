This project is a test meant as a foundation for future image detection projects

It is a very simple program: you upload a file or screenshot, and using OCR Tesseract, it reads all the text and displays it in a text box so you can copy it
